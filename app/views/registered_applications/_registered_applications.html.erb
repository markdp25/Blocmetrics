<% current_user.registered_applications.each do |app| %>
  <%= link_to "", registered_application_path(app), method: :delete, class: 'glyphicon glyphicon-remove-circle'%>
  <%= link_to app.name, registered_application_path(app) %>
  <h5><small><%= app.url %></small></h5>
<% end %>
